/**
 * Barrel export for the Busal OS design token foundation.
 *
 * Prefer importing from `@/styles` so the token surface stays explicit:
 *   import { colors, radius, designTokens } from "@/styles";
 */
export {
  colors,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
  spacing,
  spacingPx,
  radius,
  shadows,
  duration,
  easing,
  breakpoints,
  breakpointsPx,
  zIndex,
  designTokens,
  type ColorToken,
  type DesignTokens,
} from "./tokens";
