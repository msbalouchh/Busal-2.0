import {
  handleCreateReservation,
  handleListReservations,
} from "@/modules/reservations/api/reservations-route-handlers";

export { handleListReservations as GET, handleCreateReservation as POST };
