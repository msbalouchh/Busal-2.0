generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// Business domain models will be added in future migrations.
// This schema establishes the Prisma + Supabase PostgreSQL connection.
