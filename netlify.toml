# Busal OS — Netlify deployment for the root Next.js application (not mobile/).

[build]
  command = "corepack enable && pnpm install --frozen-lockfile && pnpm build"

[build.environment]
  NODE_VERSION = "22"
  NODE_OPTIONS = "--max-old-space-size=4096"